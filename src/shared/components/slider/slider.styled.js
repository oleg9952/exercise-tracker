import styled from 'styled-components/native';
import {withSideInsets} from '../../styles/mixins';

export const SliderWrapper = styled.View`
  ${withSideInsets({value: -20, type: 'margin'})}
`;

export const FlatList = styled.FlatList``;
